<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a6105160.projethockey">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.GestionEquipesActivity" />
        <activity android:name=".Activities.GestionJoueursActivity" />
        <activity android:name=".Activities.GestionPartiesActivity" />
        <activity android:name=".Activities.CreerJoueurActivity" />
        <activity android:name=".Activities.feuille_pointageActivity" />
        <activity android:name=".CreerPartiesActivity" />
        <activity android:name=".Activities.CreerEquipeActivity"></activity>
    </application>

</manifest>